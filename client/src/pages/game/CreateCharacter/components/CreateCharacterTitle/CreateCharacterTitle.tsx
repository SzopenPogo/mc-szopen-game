import classes from './CreateCharacterTitle.module.scss';

const CreateCharacterTitle = () => {
  return (
    <h1 className={classes['create-character-title']}>
      Create new character
    </h1>
  )
}

export default CreateCharacterTitle;